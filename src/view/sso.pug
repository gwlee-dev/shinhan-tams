doctype html
html.h-100(lang="ko")
    head
        include layout/meta
        block layout/head
        if title
            title= `TAMS | ${title}`
        else
            title TAMS
    body.d-flex.flex-column.h-100
        .preloader.loading
            .bar(role="progressbar")
        .postloader
        main.flex-grow-1.d-flex
            .container.pt-5.pb-6.flex-grow-1.d-flex
                .vstack.my-auto.text-center
                    a(href="/")
                        img.mb-4(src="/src/img/logo_grey.png")
                    h6 연결 세션이 종료되었습니다.
                    h6 SSO 연결을 다시 시도해 주세요.
        script(src="/src/js/app.js", type="module", defer="false")
